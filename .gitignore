*.pyc
*.pyo
*.pyd
__py
**/__pycache__/
.DS_Store
__MACOSX

# Model weights - too large for GitHub, distribute via Releases
weights/*.pt
weights/*.pth
weights/*.zip
weights/*.onnx
weights/coreml/
weights/tflite/

# Android build outputs and machine-local config
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/*.iml
android/app/*.iml
android/.kotlin/
*.apk
*.aab

# Model assets are symlinks into the gitignored weights/tflite/ dir.
# The .tflite binaries are distributed via Releases, not committed.
android/app/src/main/assets/*.tflite
